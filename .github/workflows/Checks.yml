name: Linting
on:
  push:
    branches:
      - ux
      - ux/*
      - fix
      - fix/*
  pull_request:
jobs:
  'Checking':
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
      - name: '        Lint'
        shell: bash
        run: |
          pnpm lint
      - name: '        Tests'
        shell: bash
        env:
          OS: ${{ matrix.os }}
        run: |
          pnpm test:unit
